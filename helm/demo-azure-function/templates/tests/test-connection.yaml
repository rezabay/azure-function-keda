apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-azure-function.fullname" . }}-test-connection"
  labels:
    {{- include "demo-azure-function.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-azure-function.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
